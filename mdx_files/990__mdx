https://code.kx.com/developer/libraries/grammar-of-graphics-data-abstraction/

# Table wrapper - KX Developer

Original URL: https://code.kx.com/developer/libraries/grammar-of-graphics-data-abstraction/

# Table wrapper

## .gg.tbl.apply

If given a table, return the table. If given a tbl.ty, return the proper
subset of the table defined in the type.

**Parameter:**

**Returns:**

## .gg.tbl.at

Index into a table abstraction by relative index

**Parameters:**

Name | Type | Description  
---|---|---  
table | table | [ .gg.tbl.ty ](../overview-typedefs/#ggtblty) |   
indices | long[] |   
  
**Returns:**

## .gg.tbl.box

Box a table if it is not already boxed

**Parameter:**

**Returns:**

## .gg.tbl.colnames

Return the columns names of the table

**Parameter:**

**Returns:**

Type | Description  
---|---  
symbol[] | column names  
  
## .gg.tbl.column

Return the column from a table at any indices specified by the abstraction

**Parameters:**

Name | Type | Description  
---|---|---  
table | table | [ .gg.tbl.ty ](../overview-typedefs/#ggtblty) |   
column | symbol | column name  
  
**Returns:**

Type | Description  
---|---  
any[] | indexed column values  
  
## .gg.tbl.indices

Return the indices of the visible table

**Parameter:**

**Returns:**

## .gg.tbl.indicesOf

Return the indices of a given value from a given table

**Parameters:**

Name | Type | Description  
---|---|---  
table | table | [ .gg.tbl.ty ](../overview-typedefs/#ggtblty) |   
column | symbol |   
val | any |   
  
**Returns:**

## .gg.tbl.isempty

Return whether a table is empty

**Parameter:**

**Returns:**

Return the meta for a table

**Parameter:**

**Returns:**

Return the meta letter for a column in a table

**Parameters:**

Name | Type | Description  
---|---|---  
table | table | [ .gg.tbl.ty ](../overview-typedefs/#ggtblty) |   
column | symbol |   
  
**Returns:**

## .gg.tbl.new

Create a new table abstraction from a source table

**Parameter:**

Name | Type | Description  
---|---|---  
raw | table |   
  
**Returns:**

## .gg.tbl.nrecords

Return the count of the table

**Parameter:**

**Returns:**

## .gg.tbl.onLoad

## .gg.tbl.rindex

Set the index list on the table abstraction with the given relative indices

**Parameters:**

Name | Type | Description  
---|---|---  
table | table | [ .gg.tbl.ty ](../overview-typedefs/#ggtblty) |   
index | long[] |   
  
**Returns:**

## .gg.tbl.transform

Return whether a table is meant to be transformed or not

**Parameter:**

**Returns:**

## .gg.tbl.unbox

Get the source table out of an abstraction

**Parameter:**

**Returns:**

